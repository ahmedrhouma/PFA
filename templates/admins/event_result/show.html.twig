{% extends 'base.html.twig' %}

{% block title %}EventResult{% endblock %}

{% block body %}
    <h1>EventResult</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_result.id }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ event_result.result }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_result_index') }}">back to list</a>

    <a href="{{ path('event_result_edit', {'id': event_result.id}) }}">edit</a>

    {{ include('event_result/_delete_form.html.twig') }}
{% endblock %}
